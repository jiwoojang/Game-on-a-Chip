// Useful functions that can be used anywhere in the game

extern unsigned short playerBitmap[100]; 
extern unsigned short blankBitmap[100];
extern unsigned short enemyBitmap[100];

extern int gameOver;

int power (int base, int exp); 
int log2Number(uint32_t v);
